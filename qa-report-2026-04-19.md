# QA Report — 2026-04-19

---

## Hour 1 — Post-developer integration pass (agents/nightly-2026-04-19)

**Run at:** 2026-04-19 ~02:00 UTC+4  
**Branch head:** 5452ef3 (after QA fix)  
**Commits on branch vs main:** 6 commits  

### Сборка

| Check | Status | Notes |
|---|---|---|
| dotnet build | ⚠️ NOT RUN | dotnet SDK not installed in this environment |
| dotnet test | ⚠️ NOT RUN | dotnet SDK not installed in this environment |
| npm run build (miniapp) | ✅ PASS | 69 modules, 332 kB JS, 0 warnings, 0 errors |
| Migrations | ✅ OK | 2 new migrations (AddLastFedAtUtc, AddLastTreatIndex), both have Designer.cs. No new migrations in tonight's commits. |
| wwwroot rebuild | ✅ OK | `src/Trale/wwwroot/` updated at 01:44 UTC+4 |

**Note:** `dotnet` is not installed in the CI environment. C# build and unit tests could not be verified. All changed files tonight were JSON content + MiniAppContentProvider.cs — no new DI wiring, endpoints, or DB migrations added.

---

### Автоматические проверки

#### ✅ #255 Pronouns/PresentTense ID collision fix (commit d68aba4) — VERIFIED

All 5 Pronouns question files now use IDs `pron{N}_q{MM}`:
- `questions.json` → pron1_q01…pron1_q18
- `questions2.json` → pron2_q01…pron2_q18
- `questions3.json` → pron3_q01…pron3_q18
- `questions4.json` → pron4_q01…pron4_q18
- `questions5.json` → pron5_q01…pron5_q18

Zero collisions between GeorgianPronouns and GeorgianPresentTense (180 unique IDs). ✅

#### ✅ #224 questions8.json aorist lemma fix (commit 30369e0) — VERIFIED

All 6 lexicon lemmas are now Future I forms with correct Russian descriptions:
- `წავიდებ` — уйду/пойду (будущ., 1л.ед.)
- `მოვიდებ` — приду (будущ., 1л.ед.)
- `შევის` — войдёт (будущ., 3л.ед.)
- `გამოვის` — выйдёт (будущ., 3л.ед.)
- `ავიდებ` — поднимусь (будущ., 1л.ед.)
- `ჩავიდებ` — спущусь (будущ., 1л.ед.)

All question tags correctly use `fut` prefix. ✅

#### ✅ #223 VoM lesson 4-5 sync (commit 30369e0) — VERIFIED

ContentProvider titles now align with question file titles:
- Lesson 4: `"Закрепление настоящего"` ↔ questions4.json `"Закрепление настоящего времени"` ✅
- Lesson 5: `"Прошедшее (аорист) движения"` ↔ questions5.json `"Прошедшее время (основы)"` ✅

Progression: L3 present → L4 reinforce present → L5 past/aorist ✅

#### 🔧 VoM internal ID collision fixed by QA (commit 5452ef3)

**Bug:** `q_like_up_01` appeared in both `GeorgianVerbsOfMovement/questions.json` (motion-1) and `questions2.json` (motion-2) — two different questions on different lemmas/options sharing the same ID.

**Risk:** Progress tracking or mistakes system might cross-contaminate data between VoM lesson 1 and lesson 2.

**Fix:** Renamed the `questions2.json` instance to `m2_q_like_up_01`. VoM now has 0 internal ID conflicts.

#### ⚠️ Pre-existing: GeorgianAlphabet cross-module ID collisions (180 IDs)

Not introduced tonight. Affects 5 module pairs:
- GeorgianAlphabetFull ↔ GeorgianAlphabetProgressive (54 IDs)
- GeorgianAlphabetEasy ↔ GeorgianAlphabetProgressive (36 IDs)
- GeorgianAlphabetCommon ↔ GeorgianAlphabetProgressive (36 IDs)
- GeorgianAlphabetProgressive ↔ GeorgianAlphabetTriples (36 IDs)
- GeorgianAlphabetProgressive ↔ GeorgianAlphabetVowels (18 IDs)

These alphabet sub-modules are alternative presentations of the same content and share question IDs intentionally (or not — needs methodology review). **Recommend filing a dedicated issue to assess impact before launch.**

#### ✅ Miniapp components — no dead code

The miniapp build succeeded cleanly; no unused component imports detected in the changed files.

#### ℹ️ font-geo usage (pre-existing, known risk)

`font-geo` class is used in 20+ places (LessonTheory, LandingScreen, Dashboard, Onboarding, MistakesResult). Font is defined in tailwind.config.js as `Noto Sans Georgian` with `Manrope, sans-serif` fallback. Pre-existing pattern — not introduced tonight. Risk of rendering issues in Telegram WebView is known; safe-sans fallback is present.

---

### Issues #224 и #223 — status check

Both issues still show as **OPEN** in GitHub. The commit (30369e0) references "fixes #224 #223" — these will auto-close when the branch is merged to main via PR.

---

### Тест-кейсы для ручной проверки

#### Тест 1: VoM Lesson 8 — лексика будущего времени
**Что проверяем:** SRS-карточки не показывают аористные формы с переводом будущего  
**Шаги:**
1. Открыть мини-апп → Глаголы движения → Урок 8
2. Начать квиз
3. Проверить 3-4 карточки — в вопросах должны встречаться формы `წავიდებ`, `მოვიდებ` (с -ებ суффиксом)
4. Убедиться, что нет форм `წავიდა`, `მოვიდა` (аорист) с переводом «пойду/приду»

**Ожидаемый результат:** Все формы — Future I. Аористных форм нет. ✅ (статически подтверждено)

#### Тест 2: VoM Уроки 4 и 5 — заголовки совпадают с темой квизов
**Что проверяем:** Пользователь видит нужный контент в каждом уроке  
**Шаги:**
1. Открыть Глаголы движения → Урок 4 «Закрепление настоящего»
2. Прочитать теорию — должна быть про настоящее (мिдис, მოდის и 6 направлений)
3. Начать квиз — вопросы должны быть про настоящее (теги: `prs`)
4. Перейти к Уроку 5 «Прошедшее (аорист)»
5. Теория — основы аориста; квиз — теги `pst`

**Ожидаемый результат:** Каждый урок внутренне согласован. ✅ (статически подтверждено)

#### Тест 3: Местоимения + Настоящее время — нет кросс-прогресса
**Что проверяем:** Прогресс в Местоимениях не засчитывается как прогресс Настоящего времени  
**Шаги:**
1. Пройти любой квиз в модуле «Местоимения»
2. Открыть «Настоящее время» — прогресс должен быть 0 (или свой независимый)
3. Ошибки из «Местоимений» не должны появляться в «Ошибках» «Настоящего времени»

**Ожидаемый результат:** Прогресс и ошибки изолированы по модулям. ✅ (статически подтверждено — IDs теперь разные)

---

### Итог

Все контентные фиксы ночи проверены статически и корректны. Одна дополнительная коллизия исправлена QA (VoM questions2.json). Dotnet build/test требует среды с .NET SDK.

---

## Hour 2 — Post-developer integration pass (agents/nightly-2026-04-19)

**Run at:** 2026-04-19 ~03:00 UTC+4  
**Branch head:** 3961a27 (L5 rework)  
**New commits since Hour 1:** 4 commits (e7625be, 4e59787, afa8421, 3961a27)

### Сборка

| Check | Status | Notes |
|---|---|---|
| dotnet build | ⚠️ NOT RUN | dotnet SDK not installed in this environment |
| dotnet test | ⚠️ NOT RUN | dotnet SDK not installed in this environment |
| npm run build (miniapp) | ✅ PASS | 70 modules (+1 vs Hour 1), 332 kB JS, 0 warnings, 0 errors |
| Migrations | ✅ NO NEW | No new files in src/Persistence/Migrations/ since Hour 1 |
| wwwroot rebuild | ✅ OK | `src/Trale/wwwroot/assets/` timestamps 02:32 UTC+4, hashes match fresh build |

---

### Автоматические проверки

#### ✅ L7-A — ProPaywall CTA subtext (commit afa8421) — VERIFIED

`ProPaywall.tsx` lines 280–285 now render `ვარსკვლავი · звезда` subtext beneath the buy button:

```tsx
{state !== 'error' && (
  <span className="font-geo font-normal text-[11px] opacity-70 leading-none">
    ვარსკვლავი · звезда
  </span>
)}
```

- Subtext present for all plan states (default, loading-invoice) ✅
- Correctly hidden on error state ✅
- Georgian learning element: user sees the word for «star» on each paywall impression ✅
- Commented on issue #267 — fix is complete, recommending close.

#### ✅ L5 — Dashboard 2-section rework (commit 3961a27) — VERIFIED

All acceptance criteria from issue #269 verified:

| AC | Status |
|---|---|
| Old 4-section layout (основы/грамматика/лексика/продвинутое) removed | ✅ |
| «старт» section: exactly 5 launch tiles (ა–ე) | ✅ |
| LaunchPathBar wired after 5th tile | ✅ |
| «Мой словарь» (ვ) always visible below LaunchPathBar | ✅ |
| «все темы» collapsed by default, tap to expand | ✅ |
| «все темы» count accurate (16 modules) | ✅ |
| localStorage persistence (`bombora_allthemes_collapsed`) | ✅ |
| All «все темы» modules ACTIVE (no greying, no «скоро») | ✅ |
| ComingSoonTile.tsx deleted, no remaining imports | ✅ |
| Dead helpers removed (UNLOCK_CHAIN, isSectionUnlocked, getSectionProgress) | ✅ |

#### ℹ️ LaunchPathBar unused 6th constant

`LaunchPathBar.tsx` has `GEO_LETTERS[5] = 'ვ'` and `LABELS[5] = 'Слов'` that are never rendered — only 5 IDs are passed from Dashboard. This is harmless dead data (not a bug). The 5-node bar matches the spec in #269. Cleanup optional post-launch.

#### ✅ Design spec — no code regression

Commits e7625be (roadmap) and 4e59787 (design spec L7) touch only `.md` files — no TypeScript/C# changes, no build impact.

---

### Тест-кейсы для ручной проверки

#### Тест 4: Dashboard — «старт» и «все темы» разделы
**Что проверяем:** Новый 2-секционный layout отображается корректно  
**Шаги:**
1. Открыть мини-апп → Dashboard
2. Видим секцию «старт / დასაწყისი» с 5 тайлами (Алфавит, Числа, Знакомство, Местоимения, Настоящее время)
3. Под тайлами — LaunchPathBar «рекомендуемый маршрут» с 5 узлами ა–ე
4. Под LaunchPathBar — тайл «Мой словарь» (ვ)
5. Нажать на «все темы» — раздел раскрывается и показывает 16 модулей
6. Закрыть апп, открыть снова → «все темы» остаётся в состоянии, в котором оставили

**Ожидаемый результат:** Структура отображается правильно, тайлы кликабельны, toggle работает

#### Тест 5: ProPaywall — subtext «ვარსკვლავი · звезда»
**Что проверяем:** Грузинское слово «звезда» видно под кнопкой покупки  
**Шаги:**
1. Открыть мини-апп → тапнуть на любой Pro-locked модуль
2. Открывается Paywall-шторка
3. Дождаться загрузки тарифов
4. Под кнопкой «Купить за N ⭐» должна быть подпись `ვარსკვლავი · звезда` (светло-серая, мелкая)
5. Проверить состояние ошибки: нажать при отключённом интернете → подпись должна исчезнуть («Попробовать снова» без subtext)

**Ожидаемый результат:** Subtext «ვარსკვლავი · звезда» виден на всех состояниях кроме ошибки

#### Тест 6: Dashboard на 375px (iPhone SE / узкий Android)
**Что проверяем:** Layout не ломается на узких экранах  
**Шаги:**
1. Открыть мини-апп в Safari/Chrome DevTools с шириной 375px
2. Прокрутить все тайлы «старт» — текст не обрезается, иконки видны
3. Раскрыть «все темы» — 16 модулей отображаются без горизонтального скролла
4. LaunchPathBar — все 5 узлов помещаются в одну строку, текст подписей не обрезается

**Ожидаемый результат:** Весь контент умещается в 375px без переполнения

---

## Hour 3 — Post-developer integration pass (agents/nightly-2026-04-19)

**Run at:** 2026-04-19 ~04:00 UTC+4  
**Branch head:** d05a4a8 (mn-kilim strips + VoM content fixes)  
**New commits since Hour 2:** 4 commits (81412d8, dfe8989, 38462b4, d05a4a8)

### Сборка

| Check | Status | Notes |
|---|---|---|
| dotnet build | ⚠️ NOT RUN | dotnet SDK not installed in this environment |
| dotnet test | ⚠️ NOT RUN | dotnet SDK not installed in this environment |
| npm run build (miniapp) | ✅ PASS | 70 modules, 332 kB JS, 0 warnings, 0 errors |
| Migrations | ✅ NO NEW | No new files in src/Persistence/Migrations/ since Hour 2 |
| wwwroot rebuild | ✅ OK | Assets at 03:33 UTC+4 — match developer commit d05a4a8 timestamp |

No C# changes this session — all commits touch .md, .tsx, .json, and .css only. dotnet build/test not required for correctness verification.

---

### Автоматические проверки

#### ✅ #276 L11-A — mn-kilim strips (commit d05a4a8) — VERIFIED

All 4 screens now have top + bottom kilim strips, exactly per design-spec L11:

| Экран | Верхняя полоса | Нижняя полоса |
|-------|---------------|---------------|
| Result.tsx | line 53 `mn-kilim` after Header | line 117 `mn-kilim opacity-70` before action bar |
| LessonTheory.tsx | line 68 `mn-kilim` after Header | line 99 `mn-kilim opacity-70` inside article scroll |
| VocabularyList.tsx | line 343 `mn-kilim` after Header | line 638 `mn-kilim opacity-70` at content end |
| Profile.tsx | line 144 `mn-kilim` after Header (NEW) | line 375 `mn-kilim opacity-70` (pre-existing) |

CSS `.mn-kilim` defined at `index.css:477` — 8px kilim SVG pattern in navy/ruby/gold. ✅

Design-spec note: LessonTheory bottom strip is inside the scrollable article, before the fixed action bar (consistent with how Result.tsx handles it — same pattern across all screens). ✅

#### ✅ #277 L11-B — «Покормить» button tap target fix (commit 38462b4) — VERIFIED

Dashboard.tsx line 202 matches spec exactly:

```
py-3 ✅ (was py-2; tap target now ≥44px)
bg-cream-tile ✅ (was bg-gold; no longer competes with module tiles)
font-semibold ✅ (was font-extrabold; lower visual weight)
border-jewelInk/60 ✅ (softer border per spec)
```

Button visual hierarchy on Dashboard hero now: modules (primary navigation) > Покормить (ghost-style retention) — correct priority. ✅

#### ✅ #272 VoM questions5.json — person labels fixed (commit d05a4a8) — VERIFIED

`theory_md` now correctly labels:
- 3sg examples (მივიდა, მოვიდა, შევიდა, გავიდა) as «он/она»
- 1sg paradigm shown separately with -ვედი suffix

Tips correctly distinguish:
- 1sg: «-ვედი: მივედი (я пошёл), მოვედი (я пришёл)…»
- 3sg: «-ვიდა: მივიდა (он пошёл), მოვიდა (он пришёл)…»

Fixes the bug where 3sg aorists were labeled as 1sg («я»). ✅

#### ✅ #273 VoM questions10.json — future forms + corrupted lemma (commit d05a4a8) — VERIFIED

`theory_md` now shows correct future forms:
```
Future (წავალ, მოვა, შევა)  ← was wrong: წავიდებ, მოდის, შევის
```

Corrupted lemma: `გდდი` → `გადადის` ✅  
All 8 lexicon lemmas are present-tense 3sg forms (მიდის, მოდის, შედის, …) with correct person-consistent glosses (пойдёт, придёт, войдёт… not пойду). ✅

#### ✅ Design spec L11 (commit dfe8989) — no code impact

New file `design-specs/L11-minankari-design-audit.md` — markdown only, no build impact. ✅

#### ✅ Dead code check — no orphaned imports

Checked: all 4 modified `.tsx` files build cleanly (70 modules, unchanged count). No component was added without being imported. ✅

#### ℹ️ Issues #276, #272, #273 still OPEN in GitHub

These will auto-close on merge to main (commits reference "closes #276", "fixes #272", "fixes #273"). No action needed.

#### ℹ️ Issue #277 — inline fix in tech-lead commit (38462b4)

#277 was fixed inline by tech-lead (not a separate developer task). Issue is still OPEN — same auto-close path via main merge. Fix is verified complete. ✅

---

### Тест-кейсы для ручной проверки

#### Тест 7: mn-kilim strips — единообразие на всех экранах
**Что проверяем:** Kilim-орнамент виден на всех launch-экранах  
**Шаги:**
1. Открыть мини-апп → Dashboard (килим уже был ✅)
2. Открыть любой модуль → Теория урока — должна быть полоска сверху под Header и снизу перед кнопкой
3. Пройти квиз → Результат — полоска сверху + снизу (над кнопками действия)
4. Открыть Мой словарь — полоска сверху + снизу содержимого
5. Открыть Мой профиль — полоска сверху + снизу

**Ожидаемый результат:** 8px орнамент из ромбов navy/ruby/gold виден на всех перечисленных экранах

#### Тест 8: «Покормить» кнопка — визуальная иерархия
**Что проверяем:** «Покормить» больше не конкурирует с модульными тайлами  
**Шаги:**
1. Открыть Dashboard → зона с маскотом Бомборой
2. Кнопка «🍖 Покормить · ⭐ N» должна иметь кремовый/нейтральный фон (НЕ жёлтый gold)
3. Визуально кнопка должна быть вторичной по отношению к тайлам-модулям
4. Тапнуть кнопку — должен открыться TreatShop
5. Проверить высоту: кнопка должна быть ≥44px (комфортный tap target)

**Ожидаемый результат:** Кнопка доступна, имеет нейтральный вид, не отвлекает от уроков

#### Тест 9: VoM Урок 5 — лица аориста
**Что проверяем:** Теория урока 5 не путает 1л и 3л аориста  
**Шаги:**
1. Открыть Глаголы движения → Урок 5 «Прошедшее время (основы)»
2. В теории: примеры მივიდა/მოვიდა/შევიდა/გავიდა должны быть помечены «он/она» (3л.ед.)
3. 1л.ед. должно быть отдельно: მივედი, მოვედი, შევედი (с -ვედი окончанием)

**Ожидаемый результат:** Лица аориста разграничены корректно

#### Тест 10: VoM Урок 10 — итоговое закрепление
**Что проверяем:** Будущее время и лемма «გადადის» корректны  
**Шаги:**
1. Открыть Глаголы движения → Урок 10 «Итоговое закрепление»
2. В теории: будущее время должно показывать «წავალ, მოვა, შევა» (не -ებ формы)
3. В лексиконе должно быть слово «გადადის» (переходить) — не «გდდი»
4. Перевод при каждой лемме должен быть 3л (приходит / уходит / войдёт), не 1л

**Ожидаемый результат:** Формы будущего корректны, лемма отображается без артефактов

---

## Hour 4 — Post-developer integration pass (agents/nightly-2026-04-19)

**Run at:** 2026-04-19 ~05:00 UTC+4  
**Branch head:** 1329680 (L8-C Onboarding hint + L8-D FirstVocabBanner)  
**New commits since Hour 3:** 5 commits (c70be16, f1b0ffd, e740bbf, 5916404, 1329680)

### Сборка

| Check | Status | Notes |
|---|---|---|
| dotnet build | ⚠️ NOT RUN | dotnet SDK not installed in this environment |
| dotnet test | ⚠️ NOT RUN | dotnet SDK not installed in this environment |
| npm run build (miniapp) | ✅ PASS | 71 modules (+1 vs Hour 3), 334.22 kB JS, 0 warnings, 0 errors |
| Migrations | ✅ NO NEW | No new files in src/Persistence/Migrations/ since Hour 2 |
| wwwroot rebuild | ✅ OK | Assets at 04:31 UTC+4 — match developer commit 1329680 timestamp |

No new C# business logic or DI wiring — MiniAppContentProvider.cs changes are pure string content (theory paragraphs). dotnet build/test not required for correctness verification of this session's changes.

---

### Автоматические проверки

#### ✅ #284 L8-C — Onboarding vocabulary sub-hint (commit 1329680) — VERIFIED

`Onboarding.tsx` now accepts optional `vocabularyCount` prop (default 0) and renders a sub-hint at lines 91–95:

```tsx
{vocabularyCount > 0 && (
  <p className="font-sans text-[12px] text-jewelInk/60 text-center mt-3 max-w-[280px] mx-auto">
    Твои переводы из бота уже в «Мой словарь» 📖
  </p>
)}
```

`App.tsx` wiring verified:
- `vocabularyCount` state initialized at 0 (line 60)
- Populated from `/api/miniapp/me` response field `vocabularyCount` (line 80)
- Passed to `<Onboarding vocabularyCount={vocabularyCount}>` (line 235)

Hint is correctly hidden for brand-new users (vocabularyCount = 0), shown only for returning Telegram bot users who have existing translations. ✅

#### ✅ #285 L8-D — FirstVocabBanner in VocabularyList (commit 1329680) — VERIFIED

New component `FirstVocabBanner.tsx` (56 lines):
- One-shot display via `localStorage.getItem('vocab_first_open') === 'seen'`
- Dismiss (×) and quiz start both call `dismiss()` → sets key to `'seen'`
- Tap target: quiz button has `py-3` (≥44px) ✅
- Design: `jewel-tile mx-4 mb-3 border-navy` — consistent with Minankari system

Integration in `VocabularyList.tsx`:
- Imported at line 8 ✅
- Rendered at line 346–348, condition: `items.length > 0 && !isStarterMode` — correctly hidden when user only has starter words ✅
- `startQuiz('all')` is a valid `VocabularyQuizMode` — confirmed at VocabularyList:159 (`function startQuiz(mode: VocabularyQuizMode)`) ✅

#### ✅ #278 VoM questions7.json — person label fixes (commit 5916404) — VERIFIED

| Question | Issue | Fix | Status |
|----------|-------|-----|--------|
| m7-q002 | მოვიდა (3sg) labeled as 2sg «ты пришёл» | options[0] → "როდის მოხვედი ჩემთან გუშინ?" (მოხვედი = 2sg aorist) | ✅ |
| m7-q005 | ავიდა (3sg) for explicit subject თქვენ (2pl) | options[0] → "ავედით" (2pl aorist) | ✅ |

Explanations updated to match: "მოხვედი = 2л.ед. прош. (ты пришёл)", "ავედით = 2л.мн. прош. (вы поднялись)". ✅

#### ✅ #281 VoM questions7.json — ხვალი→დილით + subject agreement (commit 5916404) — VERIFIED

`m7-q010` question now reads:  
`«ისინი მოვიდნენ ჩემთან დილით და დაჯდნენ მაგიდაზე»`
- `დილით` (утром) ✅ — was `ხვალ/ხვალი` (завтра)
- `დაჯდნენ` (3pl, they sat) ✅ — was `დაჯდა` (3sg, he sat) — now agrees with plural subject ისინი
- Correct answer: "они пришли ко мне утром и сели за столом" ✅
- options[1..3] all updated to reflect "утром" context, no "завтра" references remain ✅

#### ✅ #280 VoM questions11.json — Wednesday→Thursday (commit 5916404) — VERIFIED

`m11-q016`: All 4 options now contain `ხუთშაბათებს` (по четвергам / Thursday-DATPL).  
Explanation: "ხუთშაბათებს = по четвергам". ✅  
Was: `ოთხშაბათს` (Wednesday) in all options — semantically wrong for the Russian prompt "по четвергам".

#### ✅ #279 VoM questions11.json — three L11 errors (commit 5916404) — VERIFIED

| Question | Issue | Fix | Status |
|----------|-------|-----|--------|
| m11-q024 | prompt said «выходить» for ჩა- preverb (= descend) | prompt now says «спускаться»; explanation + tag fixed | ✅ |
| m11-q026 | შევდიოდი (1sg with -ვ-) for subject შენ (2sg) | options[0] → "შედიოდი შხაპში" (2sg, no -ვ-); explanation notes -ვ- distinction | ✅ |
| m11-q028 | blank "___" in choose_translation_rus template + non-existent საზურმე | Completely rewritten: "მე ყოველ საღამო სახლში ვბრუნდებოდი გვიანი ღამით", correct answer "я каждый вечер возвращался домой поздно ночью" | ✅ |

#### ✅ #282 MiniAppContentProvider.cs — L11 imperfect paradigm expanded (commit 5916404) — VERIFIED

Paradigm at `MiniAppContentProvider.cs:617–623` now has all 6 forms:

```
მე — მივდიოდი (1л. — с -ვ-)
შენ — მიდიოდი (2л. — без -ვ-!)   ← NEW
ის — მიდიოდა (3л.)
ჩვენ — მივდიოდით (1мн. — с -ვ-)
თქვენ — მიდიოდით (2мн. — без -ვ-)  ← NEW
ისინი — მიდიოდნენ (3мн.)
```

Pedagogical note added: "Важно: 1л. и 2л. различаются наличием -ვ-: у первого лица -ვ- есть, у второго нет." ✅  
3rd person forms corrected: `მივდიოდა` → `მიდიოდა`, `მივდიოდნენ` → `მიდიოდნენ` (3rd person has no -ვ- marker). ✅

#### ℹ️ #283 L8-B — Returning user /start backend update — NOT YET IMPLEMENTED

Issue #283 (P1): update Telegram bot's `/start` response when `MiniAppEnabled=true` and user is in Georgian mode. This requires changes to a BotCommand handler in C# (`src/Infrastructure/Telegram/BotCommands/`). Developer implemented #284 and #285 (UI changes) this hour but skipped #283 (backend). Still open, no regression introduced.

#### ✅ Design spec and markdown commits — no code impact

- `c70be16` (product): ROADMAP/STRATEGY updates only — no TypeScript/C# ✅
- `f1b0ffd` (design): `design-specs/L8-production-rollout.md` — new markdown only ✅
- `e740bbf` (tech-lead): ROADMAP/STRATEGY updates + CTA audit notes — no code ✅

---

### Тест-кейсы для ручной проверки

#### Тест 11: Onboarding — sub-hint для возвращающегося пользователя (L8-C)
**Что проверяем:** Пользователь с переводами из бота видит подсказку про словарь  
**Шаги:**
1. Использовать бот для перевода ≥1 слова (или войти с аккаунтом, у которого есть переводы)
2. Открыть мини-апп впервые (или очистить localStorage `bombora_level`)
3. Появляется экран онбординга с Бомборой и двумя кнопками выбора уровня
4. Под кнопками должна быть серая подпись: «Твои переводы из бота уже в «Мой словарь» 📖»

**Ожидаемый результат:** Подсказка видна для пользователей с vocabularyCount > 0; скрыта для новых пользователей

#### Тест 12: VocabularyList — FirstVocabBanner при первом открытии (L8-D)
**Что проверяем:** Баннер показывается один раз, запускает квиз или закрывается крестиком  
**Шаги:**
1. Убедиться, что в боте есть ≥1 перевод
2. Очистить localStorage ключ `vocab_first_open` (или открыть на устройстве впервые)
3. Открыть мини-апп → «Мой словарь»
4. Над основным контентом должен появиться баннер «Твои переводы уже здесь» с кнопкой «Начать квиз» и ×
5. Нажать «Начать квиз» → должен открыться квиз по всем словам (mode='all')
6. Вернуться в словарь → баннер НЕ должен показываться снова
7. Повторить пп. 2–3: нажать × → баннер закрывается; при следующем открытии — не появляется

**Ожидаемый результат:** Баннер one-shot, оба действия (квиз и крестик) запоминают состояние

#### Тест 13: VoM Урок 7 — лица аориста
**Что проверяем:** Вопросы 2 и 5 корректно различают 2л.ед. и 2л.мн.  
**Шаги:**
1. Открыть Глаголы движения → Урок 7
2. Найти вопрос про «Когда ты пришёл ко мне вчера?» — правильный ответ должен содержать `მოხვედი` (не мოვედი или მოვიდა)
3. Найти вопрос про подъём по лестнице с subject `თქვენ` — правильный ответ должен быть `ავედით` (2л.мн.)
4. Найти вопрос про ισινι+πришли утром — должно быть «утром» (не «завтра»), глагол `დაჯდნენ` (мн.ч.)

**Ожидаемый результат:** Формы соответствуют лицу субъекта

#### Тест 14: VoM Урок 11 — имперфект: лица и прошедшее несовершённое
**Что проверяем:** Теория показывает 6 форм; вопросы корректны после фикса  
**Шаги:**
1. Открыть Глаголы движения → Урок 11 «Прошедшее несовершённое»
2. В теории: убедиться, что показаны ВСЕ 6 форм от 1л.ед. до 3л.мн., включая 2л.ед. (შენ — მიდიოდი, без -ვ-) и 2л.мн. (თქვენ — მიდიოდით)
3. Начать квиз: найти вопрос про «бабушку по четвергам» — должно быть `ხუთშაბათებს` (не среда)
4. Найти вопрос с preverb ჩა- — подсказка «спускаться» (не «выходить»)
5. Найти вопрос с subject შენ (входил в душ) — правильный ответ `შედიოდი` (без -ვ-)

**Ожидаемый результат:** Теория полна, вопросы лингвистически корректны

---

## Hour 7 — Post-developer integration pass (agents/nightly-2026-04-19)

**Run at:** 2026-04-19 ~07:00 UTC  
**Branch head:** e8562a5 (after QA fix)  
**Commits since hour 4 QA:** 4 commits (f565406 product, 4e9d5bf design, 51bd6ed tech-lead, 6c6bfb0 fix(content))

### Сборка

| Check | Status | Notes |
|---|---|---|
| dotnet build | ⚠️ NOT RUN | dotnet SDK not installed in this environment |
| dotnet test | ⚠️ NOT RUN | dotnet SDK not installed in this environment |
| npm build | ✅ PASS | 71 modules, 334KB JS, 707ms |
| Migrations | ✅ OK | All 25 migrations have Designer.cs counterparts |

### Баги найдены и исправлены

#### 🐛 BUG FIXED: questions2.json — invalid JSON (curly/smart quotes)

**Severity:** Critical — file unparseable by JSON.parse()  
**Root cause:** Developer's fix for #289 (q_like_city_center_01) introduced Unicode smart quotes U+201C `"` / U+201D `"` as JSON structural delimiters on lines 1133-1143, likely from a text editor with auto-smart-quote enabled.  
**Impact:** Any code path loading questions2.json at runtime would throw a parse error, crashing VoM Урок 2 entirely.  
**Fix:** Replaced 11 lines of curly-quote JSON keys/values with ASCII straight-quote equivalents.  
**Commit:** e8562a5 `fix(content): repair JSON syntax error in questions2.json`  
**Verification:** `JSON.parse()` now returns valid object with 87 questions.

### Автоматические проверки

#### ✅ Content fixes from 6c6bfb0 — all 5 verified correct

| Issue | Fix | Verified |
|---|---|---|
| #286 questions3.json — impossible lemma «გადავიდის» | → «გადადის» | ✅ good lemma present, bad absent |
| #287 questions4.json m4-q007 — «გადიხართ» | → «გადადიხართ» | ✅ correct 2pl form is answer |
| #288 questions6.json m6-q007 — «დაბრუნდით» (2pl used as 1pl) | → «დავბრუნდით» (1pl with -ვ-) | ✅ answer contains -ვ- |
| #289 questions2.json — Hindi जाना + nonexistent «მივვლა» removed | → «სიარული» | ✅ no Hindi, no მივვლა |
| #290 questions6.json m6-q009 — corrupted prompt «ხილი ოხალა / გეშე» | → «გუშინ საღამოს» | ✅ no corruption tokens |

#### ✅ L8-C Onboarding vocabulary hint (from hour 5, commit 1329680)

- `Onboarding` component accepts `vocabularyCount?: number` prop
- Hint «Твои переводы из бота уже в «Мой словарь»» shown when `vocabularyCount > 0`
- Component renders correctly in build

#### ✅ L8-D FirstVocabBanner (from hour 5, commit 1329680)

- `FirstVocabBanner.tsx` exists and exported
- Imported in `VocabularyList.tsx:8` and rendered at line 347
- Passes `onStartQuiz={() => startQuiz('all')}` callback

#### ⚠️ font-geo class usage in LessonTheory.tsx and LandingScreen.tsx

- 3 usages in LessonTheory.tsx (lines 168, 190, 202)
- 7+ usages in LandingScreen.tsx
- **Known risk**: `font-geo` may not render in Telegram WebView — was flagged in previous QA hours. Not new this hour. Still unresolved. Escalated in prior reports.

#### ℹ️ No new migrations this nightly session

No `.cs` migration files were added tonight (last migration: 20260418144155_AddLastTreatIndex, from previous nightly). No migration action needed.

#### ℹ️ Issues #286-#290 — fixed in code, still open in GitHub

These issues won't auto-close until the nightly branch merges to main. Content fixes are confirmed applied.

#### ℹ️ Issue #291 (#38-A DialogOfDayCard) — OPEN, not implemented yet

Created by tech-lead hour 6. Design spec exists (`design-specs/38-dialog-of-the-day.md`). No developer implementation yet — expected in future developer hour.

### Тест-кейсы для ручной проверки

#### Тест 15: VoM Урок 2 — JSON парсится корректно
**Что проверяем:** Урок загружается без ошибок после исправления кавычек  
**Шаги:**
1. Открыть мини-апп → «Все темы» → «Глаголы движения»
2. Перейти к Уроку 2 («Направление — приставки»)
3. Дождаться загрузки теории — не должно быть экрана ошибки
4. Начать квиз — вопросы должны загрузиться
5. Найти вопрос «Люблю ходить в центр города» — правильный ответ `სიარული`  
**Ожидаемый результат:** Урок 2 открывается без ошибки; отглагольное существительное `სიარული` (масдар) — единственный верный ответ

#### Тест 16: VoM Урок 3 — корректная лемма გადადის
**Что проверяем:** В лексиконе показана существующая форма  
**Шаги:**
1. Открыть Урок 3 («Настоящее время»)
2. Убедиться, что в теории/словаре показан глагол «გადადის» (не «გადავიდის»)  
**Ожидаемый результат:** Форма «გადავიდის» нигде не появляется

#### Тест 17: VoM Урок 4 — форма 2л.мн. настоящего
**Что проверяем:** m4-q007 использует полную редуплицированную форму  
**Шаги:**
1. Открыть Урок 4 и найти вопрос о «переходе» в настоящем времени (2л.мн.)
2. Правильный ответ должен быть «გადადიხართ» (не «გადიხართ»)  
**Ожидаемый результат:** Форма с полным гадА- превербом

#### Тест 18: VoM Урок 6 — лицо аориста
**Что проверяем:** m6-q007 использует 1л.мн. (мы вернулись) с -ვ-  
**Шаги:**
1. Открыть Урок 6 и найти вопрос о возвращении
2. Правильный ответ — «დავბრუნდით» (1л.мн., с -ვ-), НЕ «დაბრუნდით» (2л.мн.)  
**Ожидаемый результат:** -ვ- маркер 1л. присутствует

---

## Hour 10 — Post-developer integration pass (agents/nightly-2026-04-19)

**Run at:** 2026-04-19 ~10:00 UTC  
**Branch head:** 2751f3b (feat(bot): Georgian returning-user /start — Fixes #283)  
**Commits since hour 7 QA:** 4 commits (ef5b448 product, 0c22dc4 design, c647c19 tech-lead, 2751f3b developer)

### Сборка

| Check | Status | Notes |
|---|---|---|
| dotnet build | ⚠️ NOT RUN | dotnet SDK not installed in this environment (consistent with all prior hours) |
| dotnet test | ⚠️ NOT RUN | dotnet SDK not installed in this environment |
| npm run build (miniapp) | ✅ PASS | 71 modules, 334 kB JS, 38 kB CSS, 0 warnings, 0 errors |
| Migrations | ✅ NO NEW | Last migration: 20260418144155_AddLastTreatIndex (previous nightly). No new migrations tonight. |
| wwwroot rebuild | ✅ OK | `src/Trale/wwwroot/assets/` timestamps updated at Apr 19 06:34 (this QA run) |

---

### Изменения этих часов (коммиты 8–9 + developer)

#### Коммит ef5b448 — product: hour 8 (triage)
- Закрыты методические issues #289, #290 (уже исправлены в hour 7)
- Отложены #292, #293, #294 (methodist, не launch-blocking)
- ROADMAP синхронизирован

#### Коммит 0c22dc4 — design(alphabet): spec #33
- Добавлена `design-specs/33-alphabet-history-cards.md` (405 строк)
- Полная спека экрана `AlphabetHistoryScreen.tsx`: 4 культурных карточки, carousel, анимации, типографика
- Не реализована — ожидает developer

#### Коммит c647c19 — tech-lead: hour 9
- Создан issue #295 (AlphabetHistoryScreen breakdown по спеке #33)
- Boy-scout fix: `App.tsx:80` — убран лишний `(as any)` для `vocabularyCount` (поле есть в `MeResponse`)
- npm run build подтверждён: 71 модуль, 0 предупреждений ✅
- ROADMAP обновлён: #33 ссылается на (issues: #295)

#### Коммит 2751f3b — feat(bot): Georgian /start — Fixes #283
- Изменён `src/Infrastructure/Telegram/BotCommands/StartCommand.cs`:
  - Новая ветка: `isGeorgianMiniApp = miniAppUrl != null && user?.Settings?.CurrentLanguage == Language.Georgian`
  - Если Georgian + MiniAppEnabled: выводит сообщение с грузинским контекстом (კარგი?, «Бомбора», «мини-апп»)
  - Клавиатура: «🏔 Открыть Бомбору» (WebApp) + «⋮ Меню в чате» (callback)
  - Остальные returning-пользователи: прежнее поведение без изменений
- Добавлены unit-тесты `tests/Infrastructure.UnitTests/BotCommands/StartCommandShould.cs`:
  - 3 теста: Georgian+MiniAppEnabled, English+MiniAppEnabled, Georgian+MiniAppDisabled
  - Проект `Infrastructure.UnitTests` включён в решение, использует NUnit/Moq/Shouldly ✅

---

### Автоматические проверки

#### ✅ #283 — StartCommand Georgian branch — ЛОГИКА ВЕРНА

Проверено статической инспекцией кода:

| Сценарий | miniAppUrl | user.Language | isGeorgianMiniApp | Результат |
|---|---|---|---|---|
| MiniAppEnabled=true, Georgian | не null | Georgian | true | Грузинское сообщение ✅ |
| MiniAppEnabled=true, English | не null | English | false | Стандартное сообщение ✅ |
| MiniAppEnabled=false, Georgian | null | Georgian | false | Стандартное сообщение ✅ |

Логика: `miniAppUrl != null && user?.Settings?.CurrentLanguage == Language.Georgian` — корректна.

#### ✅ App.tsx vocabularyCount — тип-фикс верен

`MeResponse` в `api.ts:41` определяет `vocabularyCount?: number`.  
Строка 80 теперь: `setVocabularyCount(meData.vocabularyCount ?? 0)` — без лишнего `(as any)`.  
npm build подтвердил: TypeScript принял тип без ошибок.

#### ✅ StartCommandShould.cs — структура тестов корректна

- Namespace: `Infrastructure.UnitTests.BotCommands` (не конфликтует с `IntegrationTests.BotCommandTests`)
- Mock `ITelegramBotClient.MakeRequestAsync` перехватывает `Text` через рефлексию — стандартный паттерн для Telegram.Bot SDK
- `RecordReferralLinkService` передаётся как `null!` — безопасно: в сценарии returning user без `ref_`-аргумента этот сервис не вызывается
- Assertions: `ShouldContain("კარგი?")`, `ShouldContain("мини-апп")`, `ShouldContain("Бомбора")` соответствуют тексту в StartCommand.cs (строки 150–157)

#### ✅ FirstVocabBanner — импорт и рендер (проверка из hour 7, подтверждена)

- `VocabularyList.tsx:8` импортирует, строка 347 рендерит — без изменений в этих часах

#### ⚠️ font-geo — продолжающийся известный риск

40+ usages в 15+ файлах. Не изменялись в этих часах. Потенциальный рендеринг-баг в Telegram WebView. Ранее эскалировано (hours 1–7), без движения. Не launch-blocker до тестов на реальном устройстве.

#### ℹ️ Design spec #33 — реализация не начата, issue #295 создан

`AlphabetHistoryScreen.tsx`, изменения `ModuleMap.tsx`, `types.ts`, `App.tsx` — всё ещё не существует.  
P2, не launch-blocking.

#### ℹ️ Issue #291 (#38-A DialogOfDayCard) — всё ещё не реализован

Design spec `design-specs/38-dialog-of-the-day.md` существует. Компонент и данные не добавлены. P2.

#### ℹ️ Issue #283 — OPEN в GitHub

Коммит `2751f3b` содержит "Fixes #283", но ветка не смержена в main — GitHub авто-закрытие не сработает до merge.

---

### Тест-кейсы для ручной проверки

#### Тест 19: Returning Georgian user /start — Georgian message
**Что проверяем:** Пользователь с Georgian language + MiniAppEnabled=true видит Georgian-специфичное сообщение  
**Шаги:**
1. Убедиться, что `MiniAppEnabled=true` в appsettings / конфиге бота
2. От аккаунта, у которого `Settings.CurrentLanguage = Georgian`, отправить `/start` в чат с ботом
3. Прочитать ответное сообщение  
**Ожидаемый результат:**
- Сообщение содержит `კარგი?` и слово «Бомбора»
- Инлайн-кнопка «🏔 Открыть Бомбору» открывает WebApp (мини-апп)
- Кнопка «⋮ Меню в чате» присутствует как вторая строка клавиатуры

#### Тест 20: Returning non-Georgian user /start — default message
**Что проверяем:** English-пользователь видит прежнее сообщение при MiniAppEnabled=true  
**Шаги:**
1. От аккаунта с English language отправить `/start`  
**Ожидаемый результат:**
- Сообщение содержит «переведу» и НЕ содержит `კარგი?`
- Кнопка «🚀 Открыть TraleBot» (WebApp) присутствует

#### Тест 21: Georgian user /start — MiniAppEnabled=false
**Что проверяем:** При отключённом MiniApp Georgian user получает стандартное сообщение  
**Шаги:**
1. MiniAppEnabled=false (или пустой HostAddress)
2. Georgian-пользователь отправляет `/start`  
**Ожидаемый результат:**
- Сообщение содержит «переведу» и НЕ содержит `კარგი?`
- Нет WebApp-кнопки


---

## Hour 13 — Post-developer integration pass (agents/nightly-2026-04-19)

**Commits since last QA (Hour 10):**
- `b0d7bcf` tech-lead: hour 12 — create #300 #301 #302 (#36 breakdown), boy-scout StartCommand
- `cfeb2f6` feat(profile): Georgian name transliteration widget — Fixes #300 #301 #302
- `e0d0d55` [developer] 2026-04-19_07-00

**New files:**
- `src/Trale/miniapp-src/src/utils/georgianizerName.ts`
- `src/Trale/miniapp-src/src/components/GeorgianNameCard.tsx`

**Modified files (this session):**
- `src/Trale/miniapp-src/src/screens/Profile.tsx` (+24 lines)
- `src/Infrastructure/Telegram/BotCommands/StartCommand.cs` (boy-scout: BuildKeyboard() helper + encoding fix)

---

### Сборка

| Проверка | Статус | Детали |
|---|---|---|
| dotnet build | ⚠️ NOT RUN | dotnet SDK not installed in this environment (consistent with all prior hours) |
| dotnet test | ⚠️ NOT RUN | dotnet SDK not available |
| npm run build (miniapp) | ✅ PASS | 73 modules (+2 vs Hour 10), 340.60 kB JS (+6.6 kB), 38.15 kB CSS, 0 errors, 0 warnings |
| Миграции | ✅ NO CHANGES | Last migration: 20260418144155_AddLastTreatIndex (unchanged). No new migrations this session. |

**npm module count delta:** 71 → 73 (+2) — expected: `georgianizerName.ts` + `GeorgianNameCard.tsx`  
**JS bundle delta:** 334 kB → 340.6 kB (+6.6 kB) — expected for two new files.

---

### Автоматические проверки

#### ✅ #300 — georgianizerName.ts — реализована полностью

- `NAME_DICT` содержит ~70 записей: ~50 русских имён + ~20 английских ✅
- Кириллический fallback map (`CYRILLIC_MAP`) — 30+ символов ✅
- Латинский fallback map (`LATIN_MAP`) — a-z ✅
- `transliterateToGeorgian()` экспортирован, используется в `GeorgianNameCard.tsx:25` ✅
- `clampNameFontSize()` экспортирован, используется в `GeorgianNameCard.tsx:71` ✅
- `nameLetterSpacingClass()` экспортирован, используется в `GeorgianNameCard.tsx:72` ✅
- Грузинский детектор через regex `[\u10A0-\u10FF]` — pass-through без транслитерации ✅
- Edge case `null`/пустая строка: возвращает `null`, компонент скрывается ✅
- Пространственные символы (`' '`, `'-'`) сохраняются в fallback-пути ✅

#### ✅ #301 — GeorgianNameCard.tsx — компонент корректен

- Импорт `Mascot` из `./Mascot`: moods `'cheer'` и `'guide'` — оба валидны по типу `Mascot.tsx:4` ✅
- Phase 1 (alphabetDone=false): Georgian + транслит-хинт + Mascot guide + «Выучи Алфавит» ✅
- Phase 2 (alphabetDone=true): Georgian-only, транслит opacity→0 (transition 400ms), Mascot cheer ✅
- Reveal badge: 2.5s таймер, fade-out 300ms, pointer-events:none ✅
- `localStorage.getItem('bombora_name_reveal_shown')` — показывается только один раз ✅
- Null-guard: при `transliterateToGeorgian` → null компонент рендерит `null` (hook safety: разбит на inner component) ✅
- `jewel-tile` + `anim-fade` классы соответствуют Minankari design system ✅

#### ✅ #302 — Profile.tsx интеграция — корректна

- `GeorgianNameCard` импортирован: `Profile.tsx:8` ✅
- `tgFirstName`: `window.Telegram?.WebApp?.initDataUnsafe?.user?.first_name` — optional chaining, безопасно в dev без Telegram ✅
- `alphabetDone`: вычисляется через `useMemo` из `catalog.modules` + `progress.completedLessons['alphabet-progressive']` — корректная логика ✅
- Позиция карточки: между «мой алфавит» (`Profile.tsx:227`) и «любимый раздел» (`Profile.tsx:289`) — соответствует дизайн-спеке ✅
- Guard: `{tgFirstName && <GeorgianNameCard ... />}` — widget скрыт в dev/browser без Telegram ✅

#### ✅ Boy-scout StartCommand.cs — рефактор корректен

- `BuildKeyboard()` helper вынесен из повторяющихся `InlineKeyboardMarkup` конструкций — DRY ✅
- Encoding fix: `U+FFFD` символы исправлены в `trialLine` строках ✅
- Логика `isGeorgianMiniApp` не изменена — тесты `StartCommandShould.cs` (3 сценария) остаются актуальными ✅

#### ⚠️ font-geo — GeorgianNameCard.tsx (продолжающийся известный риск)

`GeorgianNameCard.tsx:88` и `GeorgianNameCard.tsx:113` используют `font-geo` для отображения грузинского текста.  
Контекст: `font-geo` = Noto Sans Georgian — это корректное использование для грузинского скрипта.  
Риск: не рендерится в некоторых Telegram WebView без системного Noto Sans Georgian.  
Статус: известный ongoing риск с Hour 1 — не launch-blocker до тестов на реальном устройстве.

#### ℹ️ OwnerDebugPanel — `bombora_name_reveal_shown` не в гранулярных сбросах

`GeorgianNameCard.tsx:59` устанавливает `localStorage.setItem('bombora_name_reveal_shown', '1')`.  
В `OwnerDebugPanel` (`Profile.tsx:776-780`) этот ключ не представлен отдельной кнопкой.  
**Но:** кнопка «Очистить весь localStorage» (`Profile.tsx:766-770`) сбрасывает все ключи, включая этот.  
Не блокирует тестирование. Можно добавить гранулярную кнопку в следующую итерацию.

#### ℹ️ Issues #300 #301 #302 — OPEN в GitHub

Коммит `cfeb2f6` содержит "Fixes #300 #301 #302", но ветка не смержена в main — авто-закрытие не сработает до merge. Ожидаемо.

#### ℹ️ Issues #295 (#33) и #291 (#38-A) — всё ещё не реализованы

`AlphabetHistoryScreen.tsx` — не создан. P2, не launch-blocking.  
`DialogOfDayCard.tsx` — не создан. P2, не launch-blocking.

---

### Тест-кейсы для ручной проверки

#### Тест 22: GeorgianNameCard Phase 1 — имя отображается с транслитерацией
**Что проверяем:** Пользователь с незавершённым Алфавитом видит своё имя по-грузински + транслит-хинт  
**Шаги:**
1. Открыть мини-апп в Telegram (с аккаунтом, где `first_name` есть, напр. «Иван»)
2. Перейти на экран «Мой профиль»
3. Найти секцию между «мой алфавит» и «любимый раздел»  
**Ожидаемый результат:**
- Карточка «моё имя по-грузински» видна
- Грузинские буквы отображаются крупно (36px для ≤6 символов)
- Под ними транслит-хинт мелким uppercase (напр. «IVANE»)
- Mascot в режиме guide
- Фраза «Выучи Алфавит — сможешь прочитать без подсказки» внизу

#### Тест 23: GeorgianNameCard Phase 2 — транслит скрывается после завершения Алфавита
**Что проверяем:** После прохождения всех 7 уроков Алфавита транслит исчезает, badge анимируется  
**Шаги:**
1. Пройти все уроки модуля «Алфавит» (alphabet-progressive)
2. Перейти на экран «Мой профиль»  
**Ожидаемый результат:**
- Транслит-хинт исчезает (opacity → 0, transition 400ms)
- Появляется badge «✓ Ты можешь прочитать своё имя!» на 2.5 сек, затем fade-out
- Mascot переключается в режим cheer
- «★ знаю 33/33» отображается в eyebrow-строке справа

#### Тест 24: GeorgianNameCard — имя не в словаре (fallback)
**Что проверяем:** Имя, которого нет в NAME_DICT, корректно транслитерируется побуквенно  
**Шаги:**
1. Telegram-аккаунт с именем «Борис» или «Клара»
2. Открыть Profile  
**Ожидаемый результат:**
- Карточка отображается с char-by-char транслитерацией
- Грузинские буквы читаемы, без артефактов

#### Тест 25: GeorgianNameCard — имя уже на грузинском
**Что проверяем:** Пользователь с грузинским именем (напр. «გიორგი») видит его без изменений  
**Шаги:**
1. Telegram-аккаунт с `first_name` = «გიორგი»
2. Открыть Profile  
**Ожидаемый результат:**
- Карточка отображается, Georgian name = «გიორგი», translit = «გიორგი» (pass-through)
- Без двойной транслитерации

#### Тест 26: GeorgianNameCard — без Telegram WebApp (dev browser)
**Что проверяем:** В dev-браузере без Telegram карточка скрыта  
**Шаги:**
1. Открыть мини-апп в обычном браузере (не в Telegram)
2. Перейти на Profile  
**Ожидаемый результат:**
- Секция «моё имя по-грузински» не видна (tgFirstName = undefined → widget скрыт)

---

## Hour 16 — QA integration pass (agents/nightly-2026-04-19)

**Коммиты этого часа:**
- `d3eeb8f` feat(share): /share bot command + referral service unit tests — Fixes #260, Refs #261
- `28b10d7` tech-lead: hour 15 — create #306 #307 #308 (#35 breakdown), boy-scout OwnerDebugPanel

### Сборка

- **dotnet build**: ⚠️ недоступен в этом окружении (без .NET SDK, как во все предыдущие часы)
- **dotnet test**: ⚠️ недоступен в этом окружении
- **npm run build**: ✅ 73 modules, 340.75 kB / 98.71 kB gzip, 0 ошибок, 0 TypeScript warnings
- **Миграции**: ✅ нет новых миграций этого часа; `20260417195732_AddReferrals.cs` + `.Designer.cs` уже были на месте из предыдущего часа

### Автоматические проверки

#### ✅ ShareCommand.cs — DI и webhook регистрация корректны

- `DependencyInjection.cs:85`: `services.AddScoped<IBotCommand, ShareCommand>()` ✅
- `CreateWebhook.cs:77`: команда `share` с описанием «Пригласить друга и получить бонус» добавлена в список команд бота ✅
- `CommandNames.Share = "/share"` и `ShareIcon = "🔗"` добавлены ✅

#### ✅ ShareCommand.cs — логика реферальной ссылки

- Referral link: `https://t.me/{botName}?start=ref_{userId}` — формат соответствует парсеру `ReferralPrefix = "ref_"` в `StartCommand.cs:22` ✅
- Share URL: `https://t.me/share/url?url={EscapeDataString(referralLink)}` — корректный нативный Telegram share URL ✅
- `Uri.EscapeDataString` используется для URL-encoding ✅
- `InlineKeyboardButton.WithUrl("Поделиться 🔗", shareUrl)` — кнопка корректно создана ✅

#### ✅ RecordReferralLinkServiceTests — 5 тестов, полное покрытие enum

- `ShouldRecordReferral_WhenNewUserComesViaValidReferralLink` ✅
- `ShouldExtendRefereeTrial_WhenReferralRecorded` (проверяет `RegisteredAtUtc < original`) ✅
- `ShouldReturnSelfReferral_WhenUserTriesToReferThemselves` ✅
- `ShouldReturnAlreadyReferred_WhenRefereeAlreadyHasReferral` ✅
- `ShouldReturnReferrerNotFound_WhenReferrerTelegramIdDoesNotExist` ✅
- `Context.Referrals` доступен через `ITraleDbContext` (проверено: интерфейс содержит `DbSet<Referral> Referrals`) ✅
- `SaveUserWithTelegramId` определён как private helper в классе теста (не требует изменений в `CommandTestsBase`) ✅

#### ✅ ShareCommandShould — 4 unit теста

- `ReplyWithReferralLink_ContainingUserId` — проверяет `ref_12345` и `trale_bot` ✅
- `ReplyMessage_ContainsShareInviteText` — проверяет «Поделись Бомборой» ✅
- `IsApplicable_ReturnTrue_ForShareCommand` ✅
- `IsApplicable_ReturnFalse_ForOtherCommands` ✅
- Mock pattern (reflection `req.GetType().GetProperty("Text")`) консистентен с `StartCommandShould.cs` ✅

#### ✅ Boy-scout: OwnerDebugPanel — `bombora_name_reveal_shown` добавлен

- `Profile.tsx:780`: `{ label: 'Сбросить имя по-грузински', fn: () => clearKeys(['bombora_name_reveal_shown'], 'name reveal') }` добавлен ✅
- Закрывает флаг из QA Hour 13 ✅

#### ⚠️ StartCommandShould — null! для referralRecorder в тестах

- `StartCommandShould.cs` передаёт `null!` для `RecordReferralLinkService` в конструктор (`StartCommand(... null!, NullLoggerFactory.Instance)`)
- Безопасно для тестируемых сценариев (returning user /start без `ref_` аргумента)
- **Риск:** если добавят тест с `ref_xxx` аргументом, получат NullReferenceException
- Рекомендация: в следующей итерации создать мок `RecordReferralLinkService` для тестов со сценарием deep-link

#### ⚠️ appsettings.example.json — `BotName` отсутствует

- `appsettings.json:26` содержит `"BotName": "trale_bot"` ✅
- `appsettings.example.json` поле `BotName` отсутствует
- `BotConfiguration.BotName` помечен как `required` — при запуске без конфига упадёт с исключением
- Не блокер (существующая конфигурация в `appsettings.json` корректна), но новые dev-среды из example-файла получат ошибку запуска
- Не критично для production (реальная конфигурация задаётся через environment variables)

#### ℹ️ Миграции — всё в порядке

- `20260417195732_AddReferrals.cs` + `.Designer.cs` — оба файла присутствуют ✅
- Новых миграций этого часа нет — `ShareCommand` не требует схемы, `RecordReferralLinkService` уже работает через существующую таблицу Referrals ✅

#### ℹ️ Issues #260, #261, #283 — реализованы, ветка не смержена

- `#260` (/share команда): реализована в `d3eeb8f`, тесты добавлены ✅
- `#261` (backend referral tracking): реализован в предыдущих часах, тесты добавлены ✅
- `#283` (L8-B returning user /start): реализован в `2751f3b`, тесты StartCommandShould добавлены ✅
- Все три issue остаются OPEN до merge в main — ожидаемо

### Тест-кейс для ручной проверки

#### Тест 27: /share команда — реферальная ссылка в боте

**Что проверяем:** Пользователь получает персональную ссылку и кнопку «Поделиться»

**Шаги:**
1. Открыть чат с TraleBot
2. Отправить `/share`
3. Прочитать полученное сообщение

**Ожидаемый результат:**
- Сообщение содержит «Поделись Бомборой с другом»
- Ссылка имеет формат `t.me/trale_bot?start=ref_{TelegramUserId}`
- Кнопка «Поделиться 🔗» открывает нативный Telegram share sheet с referral URL

#### Тест 28: /share → /start ref_X — реферальная цепочка

**Что проверяем:** Новый пользователь, пришедший по реферальной ссылке, получает бонус и записывается в таблицу Referrals

**Шаги:**
1. Пользователь A отправляет `/share` → получает `t.me/trale_bot?start=ref_A`
2. Пользователь B (новый, не зарегистрированный) открывает ссылку → бот получает `/start ref_A`
3. Пользователь B должен получить приветственное сообщение с бонусной строкой

**Ожидаемый результат:**
- Сообщение пользователю B содержит «бонус: 60 дней бесплатно вместо 30»
- В таблице Referrals создаётся запись `ReferrerUserId=A, RefereeUserId=B`
- `B.RegisteredAtUtc` смещён на -30 дней (расширение trial)

#### Тест 29: /start ref_self — самореферал заблокирован

**Что проверяем:** Пользователь не может засчитать себе реферал

**Шаги:**
1. Пользователь A отправляет `/share` → получает ссылку с `ref_{A.TelegramId}`
2. Пользователь A сам открывает эту ссылку или отправляет `/start ref_{A.TelegramId}`

**Ожидаемый результат:**
- Нет новой записи в Referrals
- Логи содержат `RecordReferralLinkResult.SelfReferral`
- Обычный welcome-back message (без бонусной строки)

